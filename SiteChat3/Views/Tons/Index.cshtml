@model IEnumerable<SiteChat3.Models.Ton>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleTon)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleTon)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTon }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTon }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTon })
        </td>
    </tr>
}

</table>
