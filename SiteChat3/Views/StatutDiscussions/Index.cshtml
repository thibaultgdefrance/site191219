@model IEnumerable<SiteChat3.Models.StatutDiscussion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleStatutDiscussion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleStatutDiscussion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdStatutDiscussion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdStatutDiscussion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdStatutDiscussion })
        </td>
    </tr>
}

</table>
