@model IEnumerable<SiteChat3.Models.Niveau>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleNiveau)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleNiveau)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdNiveau }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdNiveau }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdNiveau })
        </td>
    </tr>
}

</table>
