@model IEnumerable<SiteChat3.Models.UtilisateurDiscussion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Discussion.TitreDiscussion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Niveau.LibelleNiveau)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Utilisateur.NomUtilisateur)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Discussion.TitreDiscussion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Niveau.LibelleNiveau)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Utilisateur.NomUtilisateur)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUtilisateurDiscussion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUtilisateurDiscussion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUtilisateurDiscussion })
        </td>
    </tr>
}

</table>
