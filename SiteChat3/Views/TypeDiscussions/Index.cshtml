@model IEnumerable<SiteChat3.Models.TypeDiscussion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibelleTypeDiscussion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LibelleTypeDiscussion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTypeDiscussion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTypeDiscussion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTypeDiscussion })
        </td>
    </tr>
}

</table>
