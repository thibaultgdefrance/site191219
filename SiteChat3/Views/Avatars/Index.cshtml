@model IEnumerable<SiteChat3.Models.Avatar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CheminAvatar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CheminAvatar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAvatar }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAvatar }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAvatar })
        </td>
    </tr>
}

</table>
