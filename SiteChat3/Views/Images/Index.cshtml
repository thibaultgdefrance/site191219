@model IEnumerable<SiteChat3.Models.Image>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomImage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CheminImage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomImage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CheminImage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdImage }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdImage }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdImage })
        </td>
    </tr>
}

</table>
